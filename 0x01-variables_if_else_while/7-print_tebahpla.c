#include <stdio.h>
/**
 * main - prints out the alphabet in lowercase and then uppercase
 * Return: 0
 */
int main(void)
{
    char z = 'z';
    while(z>='a'){
        putchar(z); 
        z--;
    }
    return (0);
}
