#include "main.h"
#include <stdio.h>

/**
  * _puts - function prints a string followed by a new line to stdout
  * @str : string to be printed
  * Return: 0
  */

void _puts(char *str)
{
	puts(str);
}
