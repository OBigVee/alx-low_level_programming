#include "main.h"
#include <stdio.h>
#include <string.h>

/**
  * _strlen - function returns the length of a string
  * @s: the string whose length is returned
  * Return: int
  */

int _strlen(char *s)
{
	return (strlen(s));
}

